package MyPackage.Players;

public class Warlock extends RangedFighter {

    public Warlock(int healthPoints, int armour, int attack, SpellType spellType, String creature) {
        super(healthPoints, armour, attack, spellType, creature);
    }
}
