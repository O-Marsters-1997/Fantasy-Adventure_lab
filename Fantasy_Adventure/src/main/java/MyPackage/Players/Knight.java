package MyPackage.Players;

import MyPackage.WeaponType;

public class Knight extends MeleeFighter {

    public Knight(int healthPoints, int armour, int attack, WeaponType weaponType) {
        super(healthPoints, armour, attack, weaponType);
    }
}
